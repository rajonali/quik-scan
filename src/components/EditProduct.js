import {useState, React, Component} from 'react';
import {Form, Button} from 'react-bootstrap';


export default function EditProduct({imageURL, id, upc, show}) {

    const handleForm = (e) => {
        e.preventDefault();
        console.log("heyy");
    }

    return (
          

    )
}